---
phase: 10-drop-in-performance-gains
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - precede_ocr.py
  - .planning/phases/10-drop-in-performance-gains/benchmark_results.md
autonomous: false
requirements:
  - RENDER-02
  - PIPE-01
  - QUAL-02

must_haves:
  truths:
    - "User has run benchmark.py and confirmed optimal DPI and worker count values"
    - "Pipeline uses the benchmark-determined optimal DPI value (hard-coded, not configurable)"
    - "Pipeline uses the benchmark-determined optimal worker count as default"
    - "Benchmark results are documented with before/after speed comparison"
  artifacts:
    - path: "precede_ocr.py"
      provides: "Optimized DPI and worker count hard-coded from benchmark results"
    - path: ".planning/phases/10-drop-in-performance-gains/benchmark_results.md"
      provides: "Documented benchmark findings per QUAL-02"
      contains: "DPI"
  key_links:
    - from: "precede_ocr.py:process_single_pdf"
      to: "get_pixmap(dpi=N)"
      via: "DPI value from benchmark"
      pattern: "get_pixmap\\(dpi=\\d+"
    - from: "precede_ocr.py:main"
      to: "max(1, N)"
      via: "Worker count from benchmark"
      pattern: "workers\\s*=\\s*"
---

<objective>
Apply benchmark-determined optimal DPI and worker count values to the pipeline, and document benchmark findings.

Purpose: Hard-code the winning DPI and worker count per D-03 and D-04 after user runs benchmarks on their actual hardware and corpus. Document results per QUAL-02.
Output: Pipeline configured with optimal values, benchmark results documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-drop-in-performance-gains/10-CONTEXT.md
@.planning/phases/10-drop-in-performance-gains/10-01-SUMMARY.md
@.planning/phases/10-drop-in-performance-gains/10-02-SUMMARY.md
@precede_ocr.py
@benchmark.py

<interfaces>
<!-- Key locations in precede_ocr.py where values will be updated -->

From precede_ocr.py - DPI value in process_single_pdf (after Plan 01):
```python
pix = page.get_pixmap(dpi=300, alpha=False)  # Update DPI here
```

From precede_ocr.py - Worker count default (line ~2080):
```python
if workers is None:
    workers = max(1, mp.cpu_count() - 1)  # Update default here
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User runs benchmarks and provides results</name>
  <what-built>
    Plan 01 swapped pdf2image for PyMuPDF rendering. Plan 02 created benchmark.py with DPI sweep (200/250/300), worker sweep (16-20), whitelist validation, and accuracy comparison. The user now needs to run the benchmarks on their actual hardware with the real corpus.
  </what-built>
  <how-to-verify>
    1. Open a terminal in the project directory
    2. Run the DPI and worker benchmarks:
       ```
       python benchmark.py "C:\path\to\your\pdf\corpus"
       ```
    3. If you have a v1.1 baseline CSV, also run with accuracy validation:
       ```
       python benchmark.py "C:\path\to\your\pdf\corpus" --baseline-csv "C:\path\to\v1.1\results.csv"
       ```
    4. Review the output tables showing:
       - DPI comparison: which DPI (200/250/300) is fastest while maintaining accuracy
       - Worker comparison: which worker count (16-20) gives best throughput
       - Whitelist impact: whether the digit whitelist actually speeds up OCR
       - Accuracy: whether PyMuPDF results match v1.1 baseline at >=94%
    5. Report back with:
       - **Winning DPI**: which DPI value to hard-code (e.g., "200" or "300")
       - **Winning worker count**: which worker count to use as default (e.g., "18")
       - **Accuracy result**: what percentage accuracy was achieved
       - **Any issues**: any errors or unexpected results from the benchmark

    Expected benchmark runtime: approximately 10-30 minutes for the full suite on 100 PDFs.
  </how-to-verify>
  <resume-signal>Provide the winning DPI value, winning worker count, accuracy percentage, and paste the benchmark output tables. Example: "DPI=250, Workers=18, Accuracy=96.2%"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply benchmark winners and document results</name>
  <files>precede_ocr.py, .planning/phases/10-drop-in-performance-gains/benchmark_results.md</files>
  <read_first>
    - precede_ocr.py (find the exact line with `get_pixmap(dpi=300` and the exact line with `workers = max(1, mp.cpu_count() - 1)`)
    - .planning/phases/10-drop-in-performance-gains/10-CONTEXT.md (D-03 and D-04 confirm hard-coding approach)
  </read_first>
  <action>
    **This task uses the benchmark results provided by the user in Task 1.**

    **Step 1 — Update DPI in process_single_pdf():**
    Per D-03: Hard-code the winning DPI value. No --dpi CLI flag.
    Find the line:
    ```python
    pix = page.get_pixmap(dpi=300, alpha=False)
    ```
    Replace `300` with the user-provided winning DPI value (e.g., `200` or `250`).
    If the winning DPI is 300 (no change needed), leave as-is and note in benchmark_results.md.

    **Step 2 — Update default worker count:**
    Per D-04: Hard-code the winning worker count as default. Existing `--workers` CLI flag remains for override.
    Find the line:
    ```python
    workers = max(1, mp.cpu_count() - 1)
    ```
    Replace with:
    ```python
    workers = N  # Optimal for 20-core hybrid CPU (8P+12E), benchmarked in Phase 10
    ```
    Where N is the user-provided winning worker count.

    **Step 3 — Create benchmark_results.md:**
    Create `.planning/phases/10-drop-in-performance-gains/benchmark_results.md` documenting:

    ```markdown
    # Phase 10 Benchmark Results

    **Date:** [date]
    **Hardware:** 20-core hybrid CPU (8 performance + 12 efficiency cores), 24 threads
    **Sample:** 100 random PDFs from corpus (seed=42)
    **Baseline:** v1.1 (pdf2image, 300 DPI, cpu_count()-1 workers)

    ## DPI Benchmark

    [Paste user's DPI comparison table]

    **Winner:** DPI [N] — [X] pages/sec ([Y]% faster than DPI 300)
    **Accuracy at winning DPI:** [Z]% (threshold: >=94%)

    ## Worker Count Benchmark

    [Paste user's worker comparison table]

    **Winner:** [N] workers — [X] PDFs/sec
    **Comparison:** [N] workers vs cpu_count()-1 ([M] workers): [improvement or degradation]

    ## Whitelist Impact

    [Paste user's whitelist comparison]

    **Finding:** Whitelist [does/does not] measurably speed up OCR with OEM 3

    ## Accuracy Validation (QUAL-01)

    **Accuracy:** [X]% ([Y]/[Z] pages match baseline)
    **Status:** [PASS/FAIL] (threshold: >=94%)
    [If any mismatches, list them]

    ## Applied Configuration

    | Setting | Before (v1.1) | After (v1.2 Phase 10) | Change |
    |---------|---------------|----------------------|--------|
    | Rendering engine | pdf2image + Poppler | PyMuPDF (fitz) | 2-12x faster |
    | DPI | 300 | [N] | [X]% faster |
    | Default workers | cpu_count()-1 | [N] | [X]% faster |
    | Whitelist | enabled | enabled | [no change / verified] |

    ## Estimated Total Speedup

    **Combined speedup:** [X]x over v1.1 baseline
    **Estimated 30K corpus time:** [Y] (down from ~70 days)
    ```

    Populate all [bracketed] values from the user's benchmark output in Task 1.
  </action>
  <verify>
    <automated>python -c "import precede_ocr; print('Module loads OK')"</automated>
    <automated>pytest tests/test_precede_ocr.py -x</automated>
  </verify>
  <acceptance_criteria>
    - precede_ocr.py contains `get_pixmap(dpi=` with the benchmark-determined DPI value (NOT necessarily 300)
    - precede_ocr.py worker default line does NOT contain `mp.cpu_count() - 1` (replaced with hard-coded value)
    - precede_ocr.py worker default line contains a comment mentioning "benchmark" or "Phase 10"
    - `.planning/phases/10-drop-in-performance-gains/benchmark_results.md` exists
    - benchmark_results.md contains "DPI Benchmark" section
    - benchmark_results.md contains "Worker Count Benchmark" section
    - benchmark_results.md contains "Accuracy Validation" section
    - benchmark_results.md contains "Applied Configuration" table
    - `pytest tests/test_precede_ocr.py -x` exits with code 0
  </acceptance_criteria>
  <done>Pipeline configured with optimal DPI and worker count from benchmarks, results documented in benchmark_results.md, all tests pass</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "import precede_ocr; print('OK')"` succeeds
2. `pytest tests/test_precede_ocr.py -x` passes all tests
3. DPI value in precede_ocr.py matches user's benchmark winner
4. Worker count default in precede_ocr.py matches user's benchmark winner
5. benchmark_results.md exists with complete documentation
6. Accuracy >= 94% confirmed in benchmark_results.md (QUAL-01)
</verification>

<success_criteria>
- Optimal DPI hard-coded in pipeline per D-03 (no --dpi CLI flag)
- Optimal worker count hard-coded as default per D-04 (--workers override preserved)
- Benchmark results documented with before/after comparison per QUAL-02
- Accuracy validated at >=94% per QUAL-01
- All tests pass with new configuration values
</success_criteria>

<output>
After completion, create `.planning/phases/10-drop-in-performance-gains/10-03-SUMMARY.md`
</output>
