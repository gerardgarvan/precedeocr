---
phase: 12-algorithmic-enhancements
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - .planning/phases/12-algorithmic-enhancements/benchmark_results.md
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "User has seen benchmark results comparing Phase 12 enhancements vs Phase 11 baseline"
    - "Rotation distribution data validates or updates the rotation order"
    - "DPI fallback coverage data validates the >70% DPI 200 success assumption"
    - "Batch rendering timing data shows speed impact"
    - "Benchmark results documented in benchmark_results.md"
  artifacts:
    - path: ".planning/phases/12-algorithmic-enhancements/benchmark_results.md"
      provides: "Phase 12 benchmark results documentation"
      contains: "Phase 12"
  key_links:
    - from: "benchmark.py"
      to: "precede_ocr.py"
      via: "Benchmark uses production pipeline functions"
      pattern: "from precede_ocr import"
---

<objective>
Run Phase 12 benchmarks on the 100-PDF sample corpus and document results.

Purpose: Validate all three Phase 12 enhancements (QUAL-01 accuracy gate, QUAL-02 benchmark documentation). This is the quality gate that confirms the enhancements are safe to ship. User needs to run the benchmark on their machine (has the corpus and Tesseract installed).

Output: Benchmark results captured and documented in benchmark_results.md.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-algorithmic-enhancements/12-CONTEXT.md
@.planning/phases/12-algorithmic-enhancements/12-01-SUMMARY.md
@.planning/phases/12-algorithmic-enhancements/12-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User runs Phase 12 benchmarks on corpus</name>
  <files>benchmark.py</files>
  <action>
CHECKPOINT: Phase 12 algorithmic enhancements (batch rendering, DPI fallback, rotation reorder) are implemented in precede_ocr.py. Benchmark functions are ready in benchmark.py. All 236 tests pass. User must run benchmarks on their machine to validate.

User runs these commands in order (each takes ~5-15 minutes on 100-PDF sample):

Step 1 - Rotation distribution:
  python benchmark.py CORPUS_DIR --rotation-dist --skip-dpi --skip-workers --skip-whitelist

Step 2 - Batch rendering:
  python benchmark.py CORPUS_DIR --batch-render --skip-dpi --skip-workers --skip-whitelist

Step 3 - DPI fallback:
  python benchmark.py CORPUS_DIR --dpi-fallback --skip-dpi --skip-workers --skip-whitelist

Step 4 - Full accuracy validation:
  python benchmark.py CORPUS_DIR --baseline-csv baseline_phase10.csv --rotation-dist --batch-render --dpi-fallback --skip-dpi --skip-workers --skip-whitelist

Step 5 - User pastes benchmark output back so it can be documented.

Resume signal: Paste the benchmark output, or type "skip" to defer to production, or "issues" with description.
  </action>
  <verify>User provides benchmark output or skip signal</verify>
  <done>User has run benchmarks and provided output data (or explicitly deferred to production)</done>
</task>

<task type="auto">
  <name>Task 2: Document benchmark results in benchmark_results.md</name>
  <files>.planning/phases/12-algorithmic-enhancements/benchmark_results.md</files>
  <read_first>
    - .planning/phases/11-advanced-config-tuning/benchmark_results.md (format reference for benchmark documentation)
    - .planning/phases/10-drop-in-performance-gains/benchmark_results.md (format reference)
    - .planning/phases/12-algorithmic-enhancements/12-CONTEXT.md (D-14: ship any improvement)
  </read_first>
  <action>
Create `.planning/phases/12-algorithmic-enhancements/benchmark_results.md` using the user's benchmark output from Task 1.

**Document structure (follow Phase 10-11 format):**

```markdown
# Phase 12: Algorithmic Enhancements - Benchmark Results

**Date:** [date]
**Sample:** 100 PDFs (seed=42)
**Baseline:** Phase 11 config (OEM 1, dict-off, DPI 200, 16 workers)

## Rotation Distribution (PIPE-02)

| Rotation | Count | Percentage |
|----------|-------|------------|
| [data from benchmark output] |

**Most common rotation:** [X]
**Recommendation:** [Keep current / Reorder to [...]]
**Action taken:** [Kept [90, 270, 0, 180] / Reordered to [...]]

## Batch Rendering (PIPE-04)

| Approach | Time (s) | Pages | IDs | ms/page |
|----------|----------|-------|-----|---------|
| [data from benchmark output] |

**Speedup:** [X.XXx]
**OOM fallbacks:** [count, if any]

## DPI Fallback (PIPE-03)

| Metric | Value |
|--------|-------|
| Total pages | [N] |
| Success at DPI 200 | [N] ([X.X]%) |
| Success at DPI 300 fallback | [N] ([X.X]%) |
| Total failures | [N] ([X.X]%) |

**D-06 validation:** [VALIDATED/FAILED] ([X.X]% >= 70% threshold)

## Accuracy Validation (QUAL-01)

**Accuracy:** [X.XX]% vs Phase 10 baseline
**Threshold:** >=94%
**Result:** [PASS/FAIL]

## Combined Phase 12 Assessment

**Incremental speedup:** [X.XXx] over Phase 11 baseline
**Accuracy maintained:** [Yes/No]
**Ship decision (per D-14):** [Ship/Revert]

## Cumulative v1.2 Performance

| Phase | Enhancement | Speedup | Accuracy |
|-------|------------|---------|----------|
| Phase 10 | PyMuPDF + DPI 200 + 16 workers | 4.34x-11.51x | 100% |
| Phase 11 | OEM 1 + dict-off | 1.01x | 100% |
| Phase 12 | Batch render + DPI fallback + rotation | [X.XXx] | [X]% |
| **Combined** | **All v1.2 optimizations** | **[X.XXx]** | **[X]%** |
```

Fill in all `[...]` placeholders with actual data from the user's benchmark output. If user typed "skip", document as "Benchmarks deferred to production validation" and note that QUAL-02 will be satisfied during production run.
  </action>
  <verify>
    <automated>python -c "import pathlib; p = pathlib.Path('.planning/phases/12-algorithmic-enhancements/benchmark_results.md'); assert p.exists(); content = p.read_text(); assert 'Phase 12' in content; assert 'PIPE-02' in content or 'Rotation' in content; print('benchmark_results.md exists and has content')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/12-algorithmic-enhancements/benchmark_results.md exists
    - File contains "Phase 12" in the title
    - File contains rotation distribution section
    - File contains batch rendering section
    - File contains DPI fallback section
    - File contains accuracy validation section
    - File contains combined assessment section
  </acceptance_criteria>
  <done>Benchmark results documented in benchmark_results.md with all Phase 12 enhancement metrics</done>
</task>

</tasks>

<verification>
1. `.planning/phases/12-algorithmic-enhancements/benchmark_results.md` exists with complete data
2. All Phase 12 enhancements benchmarked independently
3. QUAL-01 accuracy gate satisfied (>=94% or documented as deferred)
4. QUAL-02 benchmark documentation complete
</verification>

<success_criteria>
- Benchmark results captured for all three enhancements
- Accuracy validated at >=94% threshold (QUAL-01)
- Results documented in benchmark_results.md (QUAL-02)
- Combined v1.2 performance assessment complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-algorithmic-enhancements/12-03-SUMMARY.md`
</output>
